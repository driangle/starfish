# Version files bumped in lockstep by the release script.
# Each line is a path relative to the repo root.
# Supported formats: package.json (npm), pyproject.toml (PEP 621).

sdks/typescript/package.json
sdks/python/pyproject.toml
servers/typescript/package.json
adapters/p5js/package.json
adapters/threejs/package.json
