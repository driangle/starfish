# Starfish Swift SDK

Swift client SDK for the Starfish real-time communication protocol (iOS/macOS).

## Verification

Before considering any changes complete, run the following checks from this directory (`sdks/swift/`):

```bash
# Compile (must pass with no errors)
swift build

# Unit tests
swift test
```

Both commands must exit with code 0. Do not skip either of them.
